// Diagram.cpp : Code relevant to diagram construction and manipulation
#include "Diagram.h"

#include "Interactions.h"
#include "Particles.h"

#include <vector>
/*
listofgroupings Diagram::generateGroupings(n1dict& dictionary) {
	std::pair<std::vector<std::vector<Particle>>, std::vector<Particle>> pairup{};
	pairup.second = m_externs;
	auto grouplist{ getGroupings(pairup) };
	for (auto thisgrouping : grouplist) {
		for (auto group : thisgrouping) {
			bool inDict{ isInDict(dictionary, group) };
		}
	}

}
*/